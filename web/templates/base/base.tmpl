<!DOCTYPE html>
<html lang="en">
    {{template "head" .}}
    <body>
        {{template "header" .}}
        {{template "right-area" .}}

        {{template "main" .}}

        {{template "footer" .}}
        {{template "js" .}}
    </body>
</html>

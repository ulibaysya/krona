log:
  level: trace
  path: stderr
server:
  address: :8080
  network: tcp
  # address: ./krona.socket
  # network: unix
service:
  templates_path: /home/ulibaysya/projects/krona/web/templates
  static:
    serve: true
    path: /home/ulibaysya/projects/krona/tests/testdata/static
  admin_panel:
    enabled: false

storage:
  type: rdbms
  rdbms:
    engine: "postgres"
    connstr: "dbname=kronadb user=krona"
  cache:
    engine: "valkey"
    connstr: "127.0.0.1:6379"
